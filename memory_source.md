# 通用 Agent 规则

## 工作方式

- 默认使用中文回复；代码注释使用与项目现有语言一致的语言。
- 主动检查任务是否真正完成，优先提高完成度，最终总结保持简洁。
- 简单任务直接执行；复杂任务先说明简短计划并继续推进。
- 仅在破坏性操作、重大方案分歧、范围不明确或不可逆影响时暂停并请求用户确认。
- 主动提出更好的建议，包括重构、项目结构、文件安排以及清理过期内容。
- 后台输出多使用清晰的标题、emoji,高亮提高可读性必要时加分割线和制表。
- 如果生成了需要检查的成果文件，使用默认应用只打开最重要的一个；不重要的文件无需打开。

## 项目与文件

- 新建的非空项目必须包含 README，并描述项目用途和主要文件；修改现有项目时，仅在缺少 README 且本次任务涉及项目结构时补充。
- 保持项目结构清晰简洁，及时清理调试文件和临时脚本；删除任何文件后都要告知用户。
- 跨项目共用的模块应注明主要调用方。
- 项目已提供 `_link(path)` 时，所有面向用户 `print` 的文件路径统一使用 `_link(path)` 包装。
- 代码保持精简，避免无必要的兜底逻辑；只回退本轮产生且确认无效的尝试，不覆盖用户已有修改。

## 数据隐私

- 读取 `.env`、`*secret*`、`*credential*`、`*.pem` 等密钥类文件时，只报告文件存在与用途，不把密钥内容原样贴进对话或上下文。
- 确需引用凭证时使用掩码，只保留前几位（如 `sk-abc***`）。
- 将数据交给模型处理前，先把真实 PII（手机号、身份证、住址等）替换为占位符；含密钥或内部系统的截图不要交给在线视觉/图片识别服务。
- 分享截图、日志、配置前先打码 API key、token 与个人敏感字段。

## Git

- 展开工作前检查 Git 状态与 diff；若当前目录不是 Git 仓库，先检查是否位于上级仓库内。
- 不擅自执行 `git init`、暂存或提交；需要初始化或提交时先获得用户授权。
- 提交时仅包含本次任务相关修改，不混入用户已有改动；空项目和临时项目不强制使用 Git。
- 仓库装有 session hook（D:/py/common/git_hooks）时，带 Agent 尾注的提交会被自动追加 `Session:` 会话 ID，属正常现象，不要删除或修改。

## 验证与排错

- 针对局部改动优先使用现有测试、最小函数调用或最小可复现输入；如创建临时测试文件，结束前清理并告知用户。
- 影响范围较小且整套流程成本可控时，运行整套流程验证。
- 如果结果未达到预期，列出完整执行链路并分析每个可能的问题点。
- 排查复杂脚本时，使用“最小可复现输入 + 直接调用目标函数 + 结构化输出或断言”。
- 总结中指出调试期间遇到的错误以及最终处理结果。
- 结束前检查验收项、测试结果、Git diff 和残留临时文件。

## 强制验收门禁

- 任何代码修改后，先运行直接针对改动的最小回归测试，再运行项目约定的完整自动化测试命令。
- 必须运行对应的端到端验收命令；函数直接调用、mock、语法检查和 dry-run 只能算局部验证，不能代替端到端验收。只有运行会产生强烈不可逆副作用时才询问用户是否运行端到端命令
- 只有所有适用验证命令退出码为 0，且输出满足预期，才可以称任务“完成”或“验证通过”。
- 因权限、GUI、外部发送、生产数据或环境限制无法完成端到端验收时，必须明确写“尚未完成验收”，列出未执行步骤和原因；不得把任务报告为完成。
- 测试失败后，必须修复并重新运行原失败测试及适用的完整测试；不得只报告局部修复结果。
- 最终总结必须列出：实际运行命令、退出码、验证范围、未覆盖范围和调试错误。
- 不得在验证失败或未完成时提交、归档或声称交付完成。

## Python

- 使用 `uv` 管理 Python 环境和依赖；项目依赖优先使用 `uv add`，临时环境或无项目配置时使用 `uv pip install`。
- 运行 Python 命令优先使用 `uv run`。
- 生成或修改 Python 脚本后立即运行并检查结果。
- 在语义合理且安全的情况下，为参数提供便于测试的非空默认值；破坏性操作不得默认执行。
- 定位默认配置或资源文件时，通过 `Path(__file__).resolve()` 向上定位项目根目录后再拼接目标路径，避免依赖运行时当前目录。

## CLI 参数

- 参数无歧义且代码能够可靠区分时，优先支持位置参数，同时保留命名参数作为显式调用方式。
- 在帮助信息中说明位置参数的对应关系；纯数字位置参数可识别为 ID 或编号类参数。

## 浏览器自动化

- 使用 Chrome DevTools MCP 或 Playwright 时，接管 `CHROME_DEVTOOLS_PORT` 指向的 Chrome 实例；未设置时使用端口 `9222`。
- 优先复用现有浏览器实例和标签页，并使用 Chrome DevTools MCP 检查页面状态与调试问题。

## 环境

- 操作系统: Windows 11
- Shell: PowerShell 7 (pwsh)，注意和 cmd.exe、Linux 下的 bash 有差异
编码: CSV 文件保存为 UTF-8 with BOM (utf-8-sig)
PowerShell 不支持 bash 风格的 heredoc (python - <<'PY')，请改用 here-string (@' ... '@) 或脚本文件。
