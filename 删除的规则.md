使用 Chrome DevTools MCP 或写 Playwright 脚本时，接管 CHROME_DEVTOOLS_PORT（未设置为 9222）对应的 Chrome 实例，复用现有标签页而不是新开标签页
最常用的mcp服务是Chrome DevTools MCP, 他是一个Model Context Protocol服务器，主要功能是让MCP客户端能够检查和调试浏览器实例
Chrome DevTools MCP是你查你看浏览器的眼睛,在写playwright时要多用它
如果生成了文件作为工作的结果,请使用默认应用打开以方便检查(每次只打开一个最重要的文件)
编码: CSV 文件保存为 UTF-8 with BOM (utf-8-sig)
**python - <<'PY' Missing file specification after redirection operator.**
**PowerShell 不支持 bash 风格的 heredoc (python - <<'PY')，请改用 here-string (@' ... '@) 或脚本文件。**
使用 python -c 时，必须注意引号层级：外层用单引号，内部 Python 用双引号。
f-string 中嵌套引号建议用 row['field'] 写法避免冲突。
输出包含制表符 \t、换行 \n 时，不要让 PowerShell 抢先解释，直接交给 Python。
使用 uv pip install安装 Python 依赖
